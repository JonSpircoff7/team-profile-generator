const employee = require('../lib/Employee')

test('Employee', () => {

})

test('returns this name', () => {
    
})

test('returns an ID', () => {
    
})

test('returns an email', () => {
    
})

test('returns role')